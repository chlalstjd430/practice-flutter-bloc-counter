import 'package:flutter/material.dart';
import 'package:practive_flutter_bloc/counter/view/counter_first/counter_first_page.dart';

class CounterApp extends MaterialApp {
  const CounterApp({Key key}) : super(key: key, home: const CounterFirstPage());
}
